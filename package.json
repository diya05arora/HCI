{
  "name": "hui-senior-care-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Accessibility-first MERN web app for elderly and low digital literacy users",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
